# mattermost-chillzone-1337bot

# run the bot
```bash
cp .env.example .env
# edit .env
docker compose up --build -d
```

# stop the bot
```bash
docker compose down
```

# tail the logs
```bash
docker compose logs -f
```